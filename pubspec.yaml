name: permission_app
description: تطبيق Flutter يطلب أذونات الملفات والرسائل SMS ويعرض زر دخول ثم رسالة مرحباً
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
